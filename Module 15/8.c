#include<stdio.h>
#include<string.h>
int main()
{   
    // initializing character data type by mentioning char before variable name.

    char ch = 'a';
    
    //initializing character in a integer data type
    int a = ch;

    //printing ascii value of the declared character
    printf("ASCII value of a is %d", a);

    return 0;
}
