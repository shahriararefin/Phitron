/*
Update the following code so that the values in array b is ‘x’ more than the corresponding values in array a. You can only write only one line of code inside the loop (in the comment section marked “Write Code Here”). 
Do not modify anything else.  
*/
#include<stdio.h>
#include<string.h>

int main(){

    char s = 'america'; //fixed
    
    /*
        This type of character data type should be in a single quotation
        and also a semicolon was missing after finishing the statement.
    */

    

    return 0;
}